Check assignment.md for app requirements 

Api and MongoDb run on docker. To Run app:
docker-compose -f docker-compose.yml -f docker-compose.override.yml up

I also added a postman collection if its easier to test API endpoints.
